server {
  port = 8080
  // time before shutdown is initiated
  preWait = "5s"
}

ediAdapter {
  scope = "api://dev-gcp.helsemelding.edi-adapter/.default"
}

apprec {
  enabled = true
  interval = "5m"
}